[Service]
# Safety envelope only. It does not enable or start the collector.
CPUQuota=25%
MemoryMax=512M
IOWeight=50
Nice=10
# A V8 abort must not leave a large persistent core artifact on the shared host.
LimitCORE=0
