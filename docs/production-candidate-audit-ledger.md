# Production-candidate audit ledger

Scope: PR #32, `feat/base-terminal-finish-pass`, production-candidate hardening.

This is the single finding ledger for the hardening pass. Final CI, staging, soak, retention and rollback evidence is appended to this file only after it is exact-SHA verified.

| ID | Surface | Severity | User impact | Technical root cause | Evidence | Applied fix | Test | Final status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-001 | Token image proxy | P1 | A trusted logo host could redirect the server fetch to an unapproved destination; a chunked oversized image could also be buffered before rejection. | Redirects were followed automatically and only the final URL was checked; the full response used `arrayBuffer()` before enforcing the byte cap. | `src/app/api/token-image/route.ts` pre-fix used `redirect: "follow"` and `arrayBuffer()`. | Follow redirects manually, revalidate every destination against the HTTPS host allowlist, cap redirects, cancel rejected bodies, and read at most 1.5 MB. Remote logo URLs are also length-bounded and fragments are stripped. | Focused static security contract plus exact-build browser suite in Actions. | Fixed locally; final Actions/staging verification pending. |
| PC-002 | Public quote/chart APIs | P2 | Oversized request bodies could consume avoidable memory; ambiguous chart sources could silently fall back. | Both routes called `request.json()` without an application byte ceiling; chart source normalization defaulted unknown input to sample mode. | `src/app/api/quote/route.ts` and `src/app/api/chart/route.ts` pre-fix request parsing. | Add a shared streaming JSON reader with early content-length and actual-byte limits, explicit 413/400 responses, bounded chart arrays, strict identifiers/addresses, and a bounded normalized rate key. | Focused contract test; exact-build API browser tests for ambiguous and oversized chart requests; Actions provider suite. | Fixed locally; final Actions/staging verification pending. |
| PC-003 | Watchlist persistence | P1 | A saved market with honestly missing change, volume, or liquidity disappeared after reload instead of remaining available with unknown values. | The storage guard required three optional market metrics to be finite numbers. | `normalizePinnedPair` rejected `undefined` for optional fields. | Accept only `undefined` or finite values, keep unknown values unknown, and preserve all supported source kinds. | Exact-build reload test deletes the optional fields and verifies all four saved markets survive; focused static contract. | Fixed locally; final Actions/staging verification pending. |
| PC-004 | Global search | P2 | Pressing Enter while an IME composition was active could select the wrong market; clearing relied on inconsistent browser chrome. | The key handler did not check composition state and the product had no owned clear control. | `TerminalSearchBox` pre-fix keyboard and input implementation. | Ignore composed key events and add a localized, focus-preserving clear control within the established token system. | Exact-build keyboard/clear browser test; TR/EN key parity contract. | Fixed locally; final visual verification pending. |
| PC-005 | Local alerts | P2 | Users could add, disable, and delete rules but could not edit them; the compact panel declared modal semantics without a modal interaction contract; stored strings were weakly bounded. | Missing edit state, mismatched `aria-modal`, and partial storage validation. | `src/components/base-terminal/AlertCenter.tsx` pre-fix control and storage logic. | Add edit/save/cancel, explicit switch semantics, reset trigger state after edits, non-modal popover semantics with outside-click/Escape/focus return, and bounded stored rule/trigger fields. | Exact-build add/edit/disable/delete and permission-gate browser test; TR/EN/static contracts; empty/populated visual evidence. | Fixed locally; final Actions/visual verification pending. |
| PC-006 | Chart source boundary | P2 | Exact Base pools discovered through GeckoTerminal or on-chain state could be rejected by the chart API even though their pool address can be queried for real OHLCV. | The chart input type and provider gate recognized only DexScreener as a live source. | Chart route/source union and `getPairChart` gate. | Recognize all existing live source kinds and use GeckoTerminal only for a live-mode exact pool address; sample mode remains unavailable and no synthetic candles are generated. | Type contract, provider/browser suite, and chart-hover visual evidence in Actions. | Fixed locally; final Actions/visual verification pending. |
| PC-007 | 360 px responsive evidence | P2 | The narrowest required phone width had no exact functional or screenshot coverage. | The matrix stopped at 390 px. | Required viewport list versus `tests/e2e/base-terminal.spec.ts`. | Add 360×800 to both overflow/console checks and TR/EN full-page visual capture. | Exact production-build browser matrix in Actions. | Fixed locally; final Actions/visual verification pending. |

## Invariants retained

- Production remains untouched and transaction execution stays environment-gated.
- Missing market values remain unknown; no zero, canonical price, tradeability, safety, event, countdown, or candle is invented.
- Same-symbol assets remain contract-first; exact pools remain distinct while one token opportunity remains deduplicated.
- Canonical pricing, provider pricing, on-chain state, liquidity evidence, quote readiness, and transaction readiness remain separate layers.
- Real wallet connection, approval, signature, swap, and transaction submission are outside this audit and are not performed.

## Final evidence

Pending exact final SHA, Actions run/artifact IDs, staging activation, detached 60-minute soak, retention result, rollback chain, and production-invariance proof.
